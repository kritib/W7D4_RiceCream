<h1>Our Products</h1>

<% @items.each do |item| %>
  <div class="well">
    <div class="row">
      <div class="span1">
        <%= image_tag(item.image_src) %>
      </div>

      <div class="span4">
        <%= link_to item.name, item_path(item) %>
      </div>
    </div>
  </div>

<% end %>